<ul>
	<% @recipes.each do |x|%>
	<li>
		<%= x.title %>
		<img src="<%=x.image %>" />
		<ul>
			<% x.ingredients.each do |ingred| %>
				<li><%=ingred%></li>
			<% end %>
		</ul>
		<%=x.instructions %>
	</li>
	<% end %>
</ul>