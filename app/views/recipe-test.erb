<ul>
	<% @recipes.each do |x|%>
	<li class="recipe">
		<%= x.title %>
		<%= x.subtitle %>
		<img class="recipe-pic" src="<%=x.image %>" />
		<ul>
			<% x.ingredients.each do |ingred| %>
				<li><%=ingred%></li>
			<% end %>
		</ul>
		<%=x.instructions %>
	</li>
	<% end %>
</ul>